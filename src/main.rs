use std::env;
use std::process;
use nix::{sys::wait::waitpid,unistd::{fork, ForkResult, write}};

fn main(){

    // let mut cit = String::new();
    // print!("introdu un nume");
    // let b1 = std::io::stdin().read_line(&mut cit).unwrap();
    // let b = Box::new(String::from("ssss ".to_owned() + &cit));

    // // cit = "ssss ".to_owned() + &cit;

    // println!("{}", b);

    

}
